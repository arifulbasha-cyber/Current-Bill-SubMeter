import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tmss.billsplitter',
  appName: 'tmss-bill-splitter',
  webDir: 'dist'
};

export default config;
